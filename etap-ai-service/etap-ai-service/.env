# Same database NestJS/Prisma writes to — this service only reads from it
# (aggregation queries for forecasting), never writes.
DATABASE_URL="postgresql://postgres:admin123@localhost:5432/etap?schema=public"

PORT=8000
