# Copy to .env.local for local development.
# Leave VITE_API_URL empty to run entirely on the built-in Tunisian demo dataset.

VITE_API_URL=http://localhost:3000
VITE_USE_MOCK=false

VITE_KEYCLOAK_URL=http://localhost:8085
VITE_KEYCLOAK_REALM=etap
VITE_KEYCLOAK_CLIENT_ID=react-client
